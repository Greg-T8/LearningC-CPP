int main() {
    const int dmv = 17;     // Declare a constant integer variable dmv and initialize it to 17
    int var = 17;           // Declare a regular integer variable var and initialize it to 17
    constexpr double max1 = 1.4 * square(dmv);      // Declare a constant expression variable max1 and initialize it to 1.4 times the square of dmv
}